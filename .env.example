# Session signing secret (required in production — use a long random string)
SESSION_SECRET=

# API port (default 3001 — must match Vite proxy in vite.config.ts)
PORT=3001

# Optional: SQLite database file path (default: ./data/astranotes.db)
# ASTRANOTES_DB_PATH=
